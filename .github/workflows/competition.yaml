name: Evaluate and Update Leaderboard

on:
  workflow_dispatch:
    inputs:
      repository:
        description: 'The student repository (e.g., org/repo)'
        required: true
        type: string
      sha:
        description: 'The commit SHA to checkout'
        required: true
        type: string
      actor:
        description: 'The GitHub username of the student'
        required: true
        type: string

permissions:
  contents: write

jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Competition Repo
        uses: actions/checkout@v4
        with:
          ref: competition

      - name: Checkout Clean Main Branch (For Anti-Cheat)
        uses: actions/checkout@v4
        with:
          ref: main
          path: original_main

      - name: Checkout Student Code
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.repository }}
          ref: ${{ inputs.sha }}
          path: student_code

      - name: Setup Pixi
        uses: prefix-dev/setup-pixi@v0.9.3
        with:
          manifest-path: student_code/pyproject.toml
          pixi-version: v0.66.0
          cache: true
          cache-write: false
          activate-environment: false
          locked: true

      - name: Check Whitelist by Username
        env:
          STUDENT_REPO: ${{ inputs.repository }}
        run: pixi run --manifest-path student_code/pyproject.toml python scripts/check_whitelist.py "${STUDENT_REPO%%/*}"

      - name: Run Anti-Cheat Check
        run: pixi run --manifest-path student_code/pyproject.toml python scripts/check_cheat.py

      - name: Run Student Evaluation
        working-directory: student_code
        run: pixi run evaluate

      # Pull latest to prevent concurrent push conflicts.
      - name: Fetch Latest Leaderboard Data
        run: git pull --rebase origin competition

      - name: Update Leaderboard TOML
        env:
          STUDENT_REPO: ${{ inputs.repository }}
        run: pixi run --manifest-path student_code/pyproject.toml python scripts/update_leaderboard.py "${STUDENT_REPO%%/*}"

      - name: Generate README
        run: pixi run --manifest-path student_code/pyproject.toml python scripts/update_readme.py

      - name: Commit and Push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🏆 Leaderboard update by ${{ inputs.actor }}"
          file_pattern: "scripts/leaderboard.toml README.md"
          commit_user_name: "github-actions[bot]"
          commit_user_email: "41898282+github-actions[bot]@users.noreply.github.com"
          commit_author: "github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"